version: 2.1

jobs:
  hello-job:
    docker:
      - image: cimg/node:17.2.0
    steps:
      - checkout
      - run: echo "TRIGGER REPO"

workflows:
  my-workflow:
    when: pipeline.event.pull_request.draft == false
    jobs:
      - hello-job
  tagged-build:
    jobs:
      - hello-job:
          filters:
            version: 2.1
