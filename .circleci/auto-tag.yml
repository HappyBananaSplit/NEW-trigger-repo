version: 2.1

jobs:
  my-job:
    docker:
      - image: cimg/node:17.2.0
    steps:
      - checkout
      - run: echo "TRIGGER REPO"

workflows:
  my-workflow:
    when: pipeline.event.github.pull_request.draft == false
    jobs:
      - my-job
