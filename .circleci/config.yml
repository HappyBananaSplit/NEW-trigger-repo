version: 2.1

jobs:
  myjob:
    docker:
      - image: cimg/node:17.9.1
    steps:
      - checkout
      - run:
          name: cat README
          command: cat README.md
      - run:
          name: echo
          command: echo "This config is defined in NEW-trigger-repo"

workflows:
  do_some_stuff:
    when: pipeline.event.name == "push" or pipeline.event.name == "api" or (pipeline.event.name == "pull_request" and pipeline.event.github.pull_request.draft == false and pipeline.event.github.pull_request.base.ref == "main")
    jobs:
      - myjob

