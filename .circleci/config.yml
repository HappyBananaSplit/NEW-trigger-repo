version: 2.1

parameters:
  workflow-filter-based-only-on-parameters:
    type: boolean
    default: false
    
jobs:
  myjob:
    docker:
      - image: cimg/node:17.9.1
    steps:
      - checkout
      - run:
          name: cat README
          command: cat README.md
      - run:
          name: echo
          command: echo "This config is defined in NEW-trigger-repo"

workflows:
  do_some_stuff:
    # when: pipeline.parameters.workflow-filter-based-only-on-parameters == false and (pipeline.event.name == "api" or (pipeline.event.name == "pull_request" and pipeline.event.github.pull_request.draft == false))
    jobs:
      - myjob

